{
  "name": "fxgocomics",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "start": "wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types",
    "lint": "biome lint .",
    "fix": "biome check . --fix"
  },
  "devDependencies": {
    "@biomejs/biome": "2.0.6",
    "@cloudflare/vitest-pool-workers": "^0.7.5",
    "typescript": "^5.5.2",
    "vitest": "~3.0.7",
    "wrangler": "^4.8.0"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ],
  "dependencies": {
    "hono": "^4.7.5",
    "zod": "^3.24.2"
  }
}
